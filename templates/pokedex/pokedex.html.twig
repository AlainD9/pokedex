{% extends 'base.html.twig' %}

{% block title %}Hello PokedexController!{% endblock %}

{% block body %}

    <h1>Pokedex</h1>

        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('app_add_to_pokedex') }}">add a pokemon</a><br><br>
        {% endif %}

        {% for pokemen in pokemon %}
            
                {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('app_edit_from_pokedex', { slug: pokemen.slug }) }}">(Edit)</a>&nbsp;-&nbsp;
                {% endif %}

                <a href="{{ path('app_pokemon', { slug: pokemen.slug }) }}">{{ pokemen.name }}</a> - {{ pokemen.number }}

                {% if is_granted('ROLE_ADMIN') %}
                    &nbsp;-&nbsp;<a href="{{ path('app_delete_from_pokedex', { slug: pokemen.slug }) }}" onclick="return confirm ('Are you sure?')">(Delete)</a>&nbsp;-&nbsp;
                {% endif %}

            <br>

        {% else %}
        
            {{ 'Mising No.' }}

        {% endfor %}

    <div class="backPaddle">
        <a href="{{ path('app_index') }}">Back to Index</a>
    </div>

{% endblock %}